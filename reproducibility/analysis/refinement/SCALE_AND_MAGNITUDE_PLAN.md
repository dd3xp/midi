# Additional descriptive checks after v0.25 review

15 September 2026. The earlier direction results and their quartiles have already been examined. These are post-review analyses, not prospective validation of earlier claims. Targets and predictions remain unchanged.

Magnitude: retain both existing Mamba comparisons and report discordant-quadrant Full-gain and Note-loss quartiles. To check whether counts depend on arbitrarily small signs, use symmetric absolute correlation-difference cutoffs 0, .01, .025, .05, .10. Require Full gain > cutoff and Note loss > cutoff; report counts over all 186 tracks and among tracks with Full gain > cutoff. These are descriptive numerical resolutions, not perceptual or statistical significance thresholds. Report the entire grid, including reductions in the headline count; no preferred threshold or new model selection.

Scale: for all eight internal predictors, evaluate both linear and ln(a+epsilon) at Gaussian sigma 0, .05, .1, .2, .5 and 1 seconds. Epsilon grid 1e-8, 1e-7 (primary), 1e-6. Smooth the full track first (reflect, truncate4, sigma converted by the stored hop), then select the original active-frame mask. Never smooth across concatenated active segments. Compute full and active correlations, preserving undefined gate active correlations at zero smoothing. Report complete coverage and all conditions. This is a view of activity regions in the same predictions, not a separation of causal contributions; smoothing still carries information across note boundaries. For scalar summary use primary epsilon and sigma0/1, with all scales available in the PDF/JSON.

Numerical verification: check unsmoothed full/active linear values against the archived per-track report; check Gaussian sigma1 primary full means against the existing manuscript export. Check epsilon changes explicitly, and do not claim invariance if new active comparisons differ. No retraining, GPU job, new dataset, public inference or adjustment to frozen folds.
