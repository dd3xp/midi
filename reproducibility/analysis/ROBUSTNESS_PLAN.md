# Follow-up after the v0.14 three-review cycle

This is a further exploratory audit, recorded before its computation. Keep the original analysis and predictions intact. Inspect target within-interval standard deviations and per-track eligible/defined note-count distributions. Recompute the original equal-track mean within-note correlation after excluding notes with target standard deviation below 1e-8, 1e-6 or 1e-4 in cached RMS units, for all eight predictors. Keep the original prediction nonconstancy rule; report all three thresholds and coverage, not a selected cutoff. The absolute thresholds are numerical sensitivity checks, not perceptual thresholds or scale-invariant exclusions. Arithmetic averaging of r remains the defined typical-track estimand; Fisher averaging is not silently substituted.

Audit source files and cached annotations for corpus-specific boundaries, especially Bach10 next-onset offsets. Summarize all four corpora separately using existing per-track values and the original paired group-bootstrap procedure; small groups are reported as descriptive. This is not evidence of a corpus causal effect or new external validation. Preserve all outcomes in the supplement.
