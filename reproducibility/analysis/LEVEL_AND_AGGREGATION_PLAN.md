# Final review-driven checks

Before computation: use the same frozen 186-track arrays and original eligible four-frame intervals. Add linear and natural-log RMSE between predicted/target note means, and linear MAE, with one mean error per track followed by equal-track averaging. The log is applied AFTER the mean within each note, with epsilon 1e-7; no gain or offset is fitted. Report all eight predictors and paired Mamba/instrument-OLS, Mamba/shared-envelope and Mamba/BiGRU differences. This supplements correlation rather than redefining it.

Check two alternative aggregations for all four primary correlations: equal piece/duplicate-group weight (mean track r within each group, then mean group); and Fisher-z track aggregation (clip r to +/-[1-1e-7], arctanh, equal-track mean, tanh). Bootstrap the same paired groups 5,000 times, seed20260911, applying each complete aggregation inside each draw. Report the original arithmetic-track estimator alongside both alternatives, including sign changes. This is exploratory sensitivity analysis after reviews, not selecting the most favorable estimator. The primary estimator remains equal-track arithmetic mean because the task evaluates a typical instrument track without weighting long tracks or large note counts more heavily. Alternative estimators answer different population questions.
